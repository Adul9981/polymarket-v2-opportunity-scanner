# AGENTS.md — 项目档案

本文件是"每个会话都必须知道的核心共识"。新会话启动时先读本文件，再读 docs/AGENTS.md（文档管理规范），然后按窗口类型读对应手册。详细内容按路径进入对应文档，本文件不展开。

## 项目是什么

Polymarket 预测市场电竞网格交易项目：从公开行情中识别可交易的市场现象，用固定金额网格策略买入/卖出，并把每笔交易沉淀为复盘知识库。真实下单复用外部仓库 `/Users/ad/Documents/polydata/polymarket_trading_bot_strategy` 的钱包能力，本仓库不持有私钥。

核心方法论是"现象 → 策略 → 验证 → 自动化"：

- 现象层 P1–P6：描述市场正在发生什么，不下单。
- 策略层 S1–S4：把现象转为可执行网格（S1 低价反转、S2 热门回撤、S3 强势小回撤、S4 持仓管理）。
- 执行模块 M1–M7：固定金额、分档买入、止盈、D2 锁盈、彩票仓、关单、复盘。
- 成熟度 L0–L4：观察→建议→模拟→小额实盘→稳定实盘，新策略必须逐级推进，不跳级。

旧命名映射：A→S1、B→S2、C→S3、D→S4、E→P5；D2/P 是执行模块，不是策略。
当前状态：任务 1（手动交易闭环）已完成；任务 2（自动扫描机会）待验收；任务 3–5 未开始；任务 6（电竞交易情报库·订阅制网站）进行中，标记为高优先级。进度以 docs/task/PROJECT_PROGRESS.md 为准，平台功能框架见 docs/task/TASK6_INTELLIGENCE_LIBRARY_PRODUCT.md。
任务 6 弹幕情报子线已落地（2026-08-18）：虎牙直播弹幕抓取/监控/分析/复盘全链路可用，
  作为平台"观众集体智慧"数据源；入口 knowledge/DANMU_README.md，规则
  knowledge/DANMU_CAPTURE_RULES.md，报告索引 reports/intel_danmu_index.html，
  弹幕×Polymarket 对接见 docs/task/DANMU_POLYMARKET_ROADMAP.md。
任务 7（交易者拆解）进行中：拆解外部交易者（当前目标 e46m3）的公开行为，沉淀可复制策略；
基础知识库 docs/forensics/KNOWLEDGE_BASE.md，策略库 docs/forensics/STRATEGY_LIBRARY.md，
逐场拆解流程 docs/forensics/DISSECTION_GUIDE.md，案例跟踪 docs/forensics/cases/README.md。

## 窗口类型（会话分工）

- 交易执行窗口：只做 V1 实盘执行，不展开产品/战略讨论。先读 docs/runbook/V1_RUNBOOK.md、docs/runbook/V1_1_TRADE_COMMAND_GUIDE.md、docs/runbook/V1_1_PROFIT_LOCK.md。
- 交易复盘窗口：处理已结束交易的复盘沉淀。先读 knowledge/README.md。
- 主窗口（默认）：策略思考、回测验证、项目推进、文档维护。先读 docs/framework/PROJECT_FRAMEWORK.md、docs/framework/PHENOMENON_STRATEGY_FRAMEWORK.md、docs/framework/STRATEGY_PATTERN_LIBRARY.md、docs/task/PROJECT_PROGRESS.md；涉及主观情报/弹幕时加读 knowledge/DANMU_README.md（弹幕情报总索引）。
- 情报采集窗口（可选）：虎牙弹幕/解说信号采集与分析。先读 knowledge/DANMU_README.md、knowledge/DANMU_CAPTURE_RULES.md、knowledge/STREAMER_PROFILES.md；产出按 DANMU_README 标准工作流落盘。
- 合约规则套利研究窗口：只读分析官方合约与市场规则，探索规则套利与白帽漏洞上报。先读 docs/forensics/CONTRACT_ARB_BRIEF.md；红线：不攻击/不盗取合约资金，代码缺陷只走 Cantina 赏金上报。
- 清单可能扩展：新增窗口类型先登记到本文件。

## 模块地图

| 目录 | 职责 | 专属规则 |
| --- | --- | --- |
| tools/ | Python 工具：执行准备 / 实盘执行 / 发现回测 | tools/AGENTS.md |
| config/ | 策略模板、现象标签、赛事白名单、风控限额 | config/AGENTS.md |
| schemas/ | 交易配置与机会候选的 JSON 字段约定 | schemas/AGENTS.md |
| runtime/ | 交易状态、打点标记、.command 启动器、日志 | runtime/AGENTS.md |
| reports/ | 回测、扫描、诊断、风控、弹幕情报报告 | reports/AGENTS.md |
| knowledge/ | 交易复盘知识库 + 主观情报库（解说信号 / 弹幕情报 / 队伍画像 / 高价值用户） | knowledge/AGENTS.md |
| docs/forensics/ | 交易者拆解域：知识库 + 策略库 + 原始数据 | docs/forensics/AGENTS.md |

文档地图（详见 docs/AGENTS.md）：

- docs/framework/PROJECT_FRAMEWORK.md：固定金额制与策略参数。
- docs/task/PROJECT_PROGRESS.md：任务 1–5 进度库，状态变更先更新它。
- docs/framework/PHENOMENON_STRATEGY_FRAMEWORK.md：P/S/M 分层总规则。
- docs/framework/STRATEGY_PATTERN_LIBRARY.md：策略模式与成熟度。
- docs/runbook/V1_RUNBOOK.md / V1_1_TRADE_COMMAND_GUIDE.md / V1_1_PROFIT_LOCK.md：V1 执行手册。
- docs/task/V2_VALIDATION_HANDOFF.md：任务 2 live 验证交接。
- docs/task/TASK2_AUTOMATION_CANDIDATE_FLOW.md：自动化候选流程设计（三层框架）。
- docs/task/INTEL_SIGNAL_LIBRARY_PLAN.md：主观情报库建设方案（解说信号 -> 画像）。
- docs/task/DANMU_POLYMARKET_ROADMAP.md：弹幕情报 × Polymarket 行情对接路线图。
- docs/data/DATA_COLLECTION_GUIDE.md：数据采集、打点与切片。
- docs/forensics/KNOWLEDGE_BASE.md：交易者拆解基础知识库（概念/规则/账号/数据源）。
- docs/forensics/STRATEGY_LIBRARY.md：从拆解沉淀的可复制策略（成熟度分级）。
- docs/forensics/DISSECTION_GUIDE.md：逐场拆解流程与反馈提升机制。
- docs/forensics/cases/README.md：已拆/待拆案例跟踪表。
- docs/research/：历史研究材料，不作现行规范，不再新增。

## 通用约定

- 文档一律中文；代码 docstring/注释以英文为主。
- 所有生成的 HTML 页面/报告（含复盘、回测、产品原型）统一采用 **SAP/Apple 风格**：
  浅色底（#f5f5f7 系）、白色圆角卡片、克制的单一强调色、系统字体栈
  （-apple-system / PingFang SC）、充足留白、卡片式信息层级、移动端优先。
  该规范为最高优先级视觉规范，适用于所有会话与窗口类型；无特殊理由不得偏离。
- 情报库最高原则（2026-08-11）：网站清清爽爽提供情报分析——界面简洁、只给事实与统计、
  每条情报可溯源；无样本不硬撑（显示"样本不足"）、无信号不硬造（显示"今日无信号"）。
- 情报库开发节奏原则（2026-08-24 用户指定，最高）：**功能宁缺毋滥，宁可少一点、
  一点一点慢慢上，也不要一次性上很多结果发现信息是错的是乱的**。
  执行细则：小步改动、每步验证（改一个点验证一个点，测试全绿再前进）；
  不一次性堆叠多个生成器/页面改动；数据流（源文件 → 生成器 → 站点）改前先想清
  覆盖关系（如 reports 源文件会覆盖站点注入内容）；信息不明确时宁可标注"待定/待确认"，
  不硬塞结论。
- 情报库主观数据边界（2026-08-18）：弹幕/解说等主观数据对外只展示聚合结论与统计，
  不裸展示弹幕流与用户身份；灰信号（假赛/剧本/卡盘质疑）只作风险标注，不作结论。
- 联赛分类标准（2026-08-24 起，最高，适用于所有情报 / 统计 / 筛选 / 展示）：
  一级 = 游戏：LoL / CS2 / Dota2 / Valorant（预留）。
  二级 = 联赛 / 赛事（LoL 细分）：LCK / LCK CL / LPL / LEC / LCP /
    KeSPA Cup / LoL 其他（LCS、LFL、TCL 等归并）；CS2 统一、Dota2 统一。
  判定优先级：标题关键词（含 <title> 提取与"K杯"等中文）-> matches.json 元数据 ->
    已知队伍反推 -> 不硬猜（未知显示"-"）。
  实现：tools/build_history_index.py 的 normalize_league；后续可按需优化
    （如 CS2 按 IEM / BLAST / EWC 细分、LoL 其他再拆分），优化前先改本标准。
- 队伍命名统一标准（2026-08-26 起，最高，适用于所有情报 / 统计 / 筛选 / 展示 / 文件名）：
  唯一权威 = docs/data/intel/team_names.json（id / 规范缩写 abbr / 全称 full / aliases）。
  规则：所有页面与列表统一用 abbr 展示（如 KRX.C、BFX.Y、DNS.C、HLE.C、GX、SHFT）；
  全称用于详情页/标题；新增队伍必须先登记本表，禁止裸用新写法。
  归一化实现：tools/build_history_index.py 的 norm_team / key_teams / canon_teams；
  同一比赛多套写法（如 "Kiwoom DRX Challengers" 与 "KRX.C"）必须合并为一行，
  禁止历史库/统计出现重复行（教训：2026-08-25 KRX.C vs BFX.Y 曾与全称写法并存两行）。
- 情报输出默认 HTML（2026-08-20 用户指定，高优先级）：用户提到"输出情报"时，
  默认以 SAP/Apple 风格 HTML 页面呈现结果（局中/复盘/汇总页均可），再附简短说明。
- 固定金额制：单场/每轮/每档买入/每档卖出/彩票仓都是固定美元金额，不按账户比例加仓。
- 执行纪律：实盘前先 dry-run；monitor-only 不新增买单；"关单"（撤未成交挂单）≠"平仓"（卖出持仓）。
- 复盘闭环：每笔真实交易结束后写复盘到 knowledge/reviews/。
- git：默认不提交、不推送，git 操作只在用户显式要求时做。

## 边界与禁区（红线）

自主执行范围（圈内）：以 config/risk_limits.json 为准。当前 autopilot 默认关闭（enabled=false），需要用户在本会话显式开启；开启后仅允许 A/B（S1/S2）自主执行，C（S3）需确认，D（S4）仅建议。限额：每日 $75、单市场 $30、并发 3 个市场，超出即停。

注：风控限额以 config/risk_limits.json 为唯一权威（当前：每日 $200、单市场 $80、并发 3），
本段旧数字已过时，勿引用。

红线（圈外，必须停下问用户）：

1. 超出风控限额；风险限额放宽必须用户确认（收紧可自主）。
2. 方向/token 解析有歧义：无法唯一确定买哪边时不猜、不下单。
3. 策略成熟度不足（S3 未验证、S4 只建议、未到 L3 的新策略）不自动实盘。
4. 异常状态：疑似重复下单、订单状态不明、数据长时间不更新、接近终局/spread 过大 → 自动暂停。
5. 绝不读取/复制/移动私钥；绝不修改 polydata 仓库，只调用其接口。
6. 主动平仓（卖出已有持仓）必须用户明确表达；按计划自动止盈/卖出除外。

## 最高优先级任务（A 级，2026-08-18 起）

情报库（任务 6）**持续功能优化与建设 = 最高优先级 A 级任务**，与数据可靠性防错同级。

```text
1. 所有窗口产出（策略 / 复盘 / 形态 / 信号 / 弹幕 / 拆解）优先向情报库沉淀。
2. 弹幕情报子线已落地（阶段 0 完成）：抓取 / 监控 / 分析 / 复盘全链路可用，
   总入口 knowledge/DANMU_README.md；与行情对照见 docs/task/DANMU_POLYMARKET_ROADMAP.md。
3. 主观数据边界（沿用通用约定）：对外只展示聚合结论与统计，不裸展示弹幕流与用户身份；
   灰信号（假赛 / 剧本 / 卡盘质疑）只作风险标注，不作结论。
4. 情报库建设不因其他任务优先级而搁置；新功能点先入框架文档再排期。
5. 弹幕情报 = 标准工作流（SOP，2026-08-19 固化）：按 `knowledge/DANMU_WORKFLOW.md`
   五阶段执行（准备→启动→监控→情报→复盘入库→交接接力）；
   多直播间首选 `tools/run_danmu_session.py`（每房独立落盘/健康状态/自动重启，
   聚合情报写 `reports/intel_danmu_live_<session>.html` 与
   `runtime/danmu_sessions/<session>/intel.json`），新会话先读同目录
   `session.json` 接力；比赛结束必做整场复盘 + 同步 `docs/data/intel/` 结构化库。
6. 比赛结束自动情报流水线（最高准则，2026-08-20 起）：一旦通过弹幕/多信号
   检测到某场比赛结束，Agent 必须**自动**完成以下全链路，不等用户指示：
   a. 输出该场情报 HTML（SAP/Apple 风格，含 LONG/SHORT 分层）；
   b. 同步结构化情报库：matches / teams / players / gray_signals /
      gray_entities / bp_signals / bp_entities / leagues（打 memory_tier）；
   c. 更新报告索引 reports/intel_danmu_index.html 与知识库 DANMU_INTEL.md；
   d. 可验证项（BP 判负、灰信号、预测、盘口）回填验证结论；
   e. 采集中断/数据缺口照常记录，不因自动而省略。
   用户在旁不需要任何提示词；用户后续消息只是补充/修正，不是触发条件。
7. 发布佐证闭环原则（2026-08-22 起，情报库最高原则）：弹幕情报页发布必须带
   "弹幕情报 -> 比赛结果 -> 验证回填"闭环（观众预测命中率 / 灰信号兑现率 /
   BP 判负验证 / 高价值用户命中）；无闭环的情报不算交付；闭环是产品差异化钩子。
   框架见 docs/task/DANMU_INTEL_FRAMEWORK.md。
8. 队伍情报库自动沉淀闭环（2026-08-26 起）：每场比赛结算（result_inferred）后，
   自动把该场各节点弹幕统计合并进 teams.json + TEAM_PROFILES.md
   （tools/accumulate_team_intel.py，幂等防重复，已接入 vps_publish）；
   跨场统计（灰信号兑现率 / BP 锚点兑现率）随画像页展示——这是"每场 -> 队伍画像"
   的复利资产，禁止只出单场页不沉淀。
8. 情报及时性标准（2026-08-26 起，最高）：小局结束 / 局内关键节点结束后
   数分钟内完成情报输出并上线（时效可稍慢，**准确与结构清晰优先**）。
   实现：检测/发布轮询 1 分钟 + **Codex 全量输出**（读取 skill/模板生成完整页，
   昨日标准）+ 并行生成（BP/局中同局并行）+ 结算状态每小局迭代实时重读。
   **快节点方案（规则层摘要+直连 DeepSeek）2026-08-26 弃用**——未经验证、
   输出结构混乱；恢复 Codex 全量路径。状态文件记录 source=codex 供监控。
9. 免费/付费边界（2026-08-26 起，最高）：**已结束比赛的实时情报对免费用户开放**
   （赛后复盘闭环 = 免费漏斗）；仅"实时进行中 / 未开赛"的节点情报为 Pro 付费。
   付费墙注入按"比赛是否已结束"判定（settlements / 全小局 closed / 结果回填），
   实现：tools/add_paywall.py + tools/vps_publish.py finished_slugs/finished_keys；
   教训：曾按文件名一刀切锁 Pro，导致已结束比赛节点页误锁（2026-08-26 修复）。
```

## 最高优先级防错规则（2026-08-16 起，全项目通用）

扫描/数据可靠性是全项目最高优先级，适用于所有窗口类型与所有自动化功能
（交易执行、情报站、拆解工具、网页产品等一律适用）。宁可慢、宁可多问，
不可再犯以下错误：

1. 空结果必须过自检：任何"扫描/抓取结果为 0/空"的结论，必须先过
   tools/task2_pipeline.py 的"空结果自检"（配套回归测试 tests/test_scan_regression.py）。
   抓取为 0、电竞标签为 0、白名单 0 匹配、时间窗口异常 → 一律告警，
   禁止报"今日无比赛/无信号"；只有自检通过才允许输出空结果结论。
2. 开赛时间字段：电竞赛事真实开赛时间以 market 层 gameStartTime 为准；
   事件层 startDate 是挂牌时间，禁止当作开赛时间。
3. 电竞抓取方式：必须按 Esports 标签（config/market_watchlist.json 的
   esports.tag_id=64）全量拉取；禁止用"全局按时间两头翻页"替代（会漏中间段）。
4. 联赛监控登记：所有要监控的联赛必须先登记白名单关键词并跑一次 live 验证。
   已登记 LoL：LCK/LPL/LCP/LEC/KeSPA Cup；CS2：IEM/BLAST/EWC；Dota2：TI/ESL One。
5. 回归测试门禁：任何改动上线前 tests/test_scan_regression.py 必须全绿；
   新增联赛/新功能同步补测试，不允许出现漏抓/空结果误报类回归。
6. 用户对数据提出异议时，优先怀疑工具：先交叉验证、先查根因，再回应；
   不得用规则解释"工具可能没错"。
7. 弹幕/主观抓取自检（2026-08-18 起）：弹幕抓取 0 条 / 长时间无新数据（连接假死）
   必须告警排查，禁止直接报"无弹幕/无信号"；断线重连后补记完整性；
   词表未覆盖的队伍/选手（如 KC/GX/Canna）分析会静默漏掉，须定期扩充词表并回归。
8. 弹幕灰信号纪律：灰信号（假赛/剧本/卡盘）只作聚合风险标注与盘口对照素材，
   不作为假赛证据；对外呈现必须写明"观众质疑，非结论"（详见 DANMU_CAPTURE_RULES.md）。
9. 结果校验自主化（2026-08-19 起，最高优先级）：结果/比分/状态类校验工作是
   Agent 的自主职责，默认不反问用户；凡能自查的一律自主闭环（多信号共振 +
   多源交叉 + 官方/比分源核对），仅当外部权威信息缺失时标注"待官方确认"。
   禁止把观众预测/玩梗当结果（2026-08-19 CS 误判教训）；判定方法见
   knowledge/VERIFICATION_METHODOLOGY.md（全项目通用），CS/弹幕细节另见
   DANMU_CAPTURE_RULES.md 第 12 节与 tools/verify_match_end.py。
10. 链接处理 SOP（2026-08-20 起，全项目通用，最高优先级）：用户直接发链接
    （推特/Polymarket/文章/视频/其他网址）时，禁止先报"网络不通/打不开"再慢慢
    排查，按以下顺序一步到位，避免每次从零排障：
    a. 先分类：账户 / 事件 / 市场 / 推文 / 文章 / 视频 / 代码仓库。
    b. 优先结构化公开数据源，不要优先开网页：
       - Polymarket 账户：用户名 -> 地址先搜公开索引（如 "<用户名> 0x" 快讯），
         拿到 0x 地址后直接 data-api /trades /activity /positions；
       - Polymarket 事件/市场：gamma /events?slug= 或 /markets?slug= + clob
         /prices-history；
       - 推文：用搜索索引/转载站交叉验证，官方无公开 API；
       - 文章/普通网页：open_page 或 curl 抓正文，失败则搜标题找转载。
    c. 沙箱内联网必失败（DNS 不通是沙箱特性，不是网站故障）：一次到位申请联网
       授权并说明用途，不要在沙箱内反复试错；能用已批准前缀就直接用。
    d. 网页若是 Next.js 等 JS 渲染，HTML 里没有数据：不硬解析页面，改用
       公开 API / 第三方索引 / 浏览器能力。
    e. 数据先小样本核对字段，再全量分片拉取（时间窗 + offset、takerOnly=false、
       内置重试退避）；原始 JSON 落盘 docs/forensics/data/<目标>/，只增不改；
       结论必须可溯源（来源 + 抓取时间）。
    f. 拿到数据后先与快讯/转载/第三方索引交叉验证再下结论，禁止凭印象编数字。
11. 情报可见性防错（2026-08-20 起，最高优先级）：数据入库 ≠ 情报已交付。
    任何入库信息（灰信号/结果/画像/BP/统计）必须同步出现在用户可见出口
    （对应比赛 HTML 页、统计页、报告索引、知识库），禁止"库里有、页面没有"。
    灰信号必须显著展示（条数/指向/纪律）并进统计页
    （reports/intel_gray_signals_stats.html）；用户口述线索（如朋友告知）
    也要落库核实，不能因"没看到弹幕"而忽略
    （教训：BRO-KRX G3 灰信号曾只入库未上页面，详见 CAPTURE_RULES 第 15bis 节）。
12. 情报关联防错 + 一次错误原则（2026-08-23 起，最高优先级）：
    a. 生成器 / 模板在引用已有情报页时，必须用真实文件名做存在性校验
       （文件名 / 路径 / 大小写 / 连字符），禁止因关联错误让已有数据"看不见"；
       "详情页 / 节点显示暂无"的结论必须先验证关联逻辑再输出。
       （教训：build_match_page 曾把 WE-LGD 拼成 WELGD -> 详情页数据未关联显示"暂无"。）
    b. 全项目大原则：情报过程中的任何错误，犯过一次即固化防错规则 / 回归测试，
       绝不允许同类错误再次发生；新错误处理流程 = 记录 -> 定位根因 -> 固化 -> 回归。
    c. 站点/线上验证必须走用户真实路径（2026-08-25 固化）：
       先确认实际访问地址与重定向行为（自定义域名后旧地址会 301 -> 新域名），
       验证用 curl -L 跟随重定向 + 检查 final URL 与实际内容关键词（+ -k 处理
       证书过渡期），禁止只测单点地址就下"未更新 / 404 / 为空"结论；
       任何"线上没有"先怀疑自己测错路径，再怀疑系统
       （教训：2026-08-25 用旧地址未跟随重定向，误判 today 页未更新）。
    d. 比赛状态与结束检测防错（2026-08-25 固化，最高优先级）：
       1) 结束检测必须有时间门槛：比赛已开始且进行 ≥30 分钟才允许
          verify_match_end，未开始/刚开赛只走赛前/局中逻辑
          （教训：GX-G2 与 CS2 场次曾在开赛前被弹幕闲聊词误判"已结束"
          并生成假复盘页）；
       2) 页面状态判断真实时间优先：start 未到=未开始；end 已过 / 事件 closed /
          已有完整情报页（服务器真实结束后产出）=已结束；禁止用扫描快照
          time_status 直接定"进行中"（教训：08-22/08-23 已结束比赛曾显示
          "进行中"）；
       3) 跨时区禁止用 UTC 日期与北京时间比较判结束（事件 start/end 是 UTC，
          页面按北京时间展示；教训：GX-G2 北京时间凌晨刚开赛曾被误判结束）；
       4) 误判产物必须清理：状态文件生成时间早于开赛 → 删除状态与报告，
          让真实结束后重新产出（工具：vps_intel_pipeline 清理逻辑）。
       5) 结果判定门禁（工具，2026-08-25 固化）：复盘/结果页发布前必过
          tools/match_state_guard.py（时间门槛 / 结构源优先 / 反讽识别 /
          比分源滞后 四道闸），回归测试 tests/test_match_state_guard.py
          （E1-E6 锁定：刚开赛误判、仅弹幕定胜负、反讽、比分源滞后、
          正常路径、跨时区）；门禁不过 = 情报未交付。
13. 数据端完备性原则（2026-08-24 起，最高优先级，数据采集层总纲）：
    弹幕情报的可靠性建立在数据完备性之上——同一场比赛，必须覆盖该联赛
    **所有已登记直播源**（跨平台多房间：虎牙 / SOOP / Twitch / KICK），
    禁止单平台 / 单房间采集。数据越多越全，交叉验证越可靠；缺源即缺口，
    缺口必须显式标注，不得静默跳过。
    a. 全覆盖：比赛确认开播 -> 查 docs/data/intel/leagues.json 该联赛默认采集集
       -> 所有 isOn 直播间一次性加入同一 run_danmu_session（跨平台可混用）；
       离线房间在完整性清单中标注"离线未采"，不算静默缺源。
    b. 同场同会话：同一场比赛的所有房间放同一 session（intel.json 聚合），
       禁止按平台拆 session（教训：FNC-NAVI G1 曾只抓 Twitch，漏掉虎牙三路）。
    c. 完整性标注：每份情报页必须标注"实际数据源 / 预期数据源 / 缺口"；
       关键结论（结果 / 灰信号 / BP 锚点 / 盘口）必须多源共振，或明确标"单源待验证"。
    d. 交叉验证：单源信号 = 待验证；两路及以上共振 = 多源确认；
       多源方向背离 = 分歧信号（更有价值，单独标注）。
    e. 补源兜底：发现漏源立即补采；补采前时段标记完整性缺口，
       赛后用 VOD 回捞（Twitch / SOOP 可）补齐；无法补齐则记录"数据缺口"。
14. 赛果判定优先级 + 弹幕反讽防误（2026-08-25 起，最高优先级）：
    a. 赛果判定优先级：官方比分源 / 比分机器人 / 权威比分站 / 用户确认
       > 弹幕情绪。弹幕只做过程佐证，禁止单独定胜负。
    b. 英文 Twitch 弹幕大量反讽 / 玩梗（"FNC ARE BACK" / "HOLY X" 可能是
       嘲 X 送或嘲老毛病；"X is playing well.. mhm" 是典型反讽），必须先做
       语气判定；无法判定时降级为"未确认"，禁止当作庆祝信号。
    c. 弹幕情绪与结构源冲突时，禁止用弹幕覆盖结构源，必须标注分歧并等官方结算。
       （教训：FNC-NAVI G1 误把英文弹幕反讽当 FNC 庆祝，判 FNC 1-0，
       实际 NAVI 1-0——比分机器人与官方比分源均正确，弹幕误读。）
15. 关键情报价值呈现机制（2026-08-26 起，最高优先级，输出层总纲）：
    速览卡 = BLUF（结论先行）× Key Judgment（信号一句话）× So-What（价值一句话）。
    a. 每条关键信息必须回答两问：发生了什么（信号，含方向与关键对象）+
       意味着什么（价值/含义，≤35 字，含方向与置信：多源确认 / 单源待验证 /
       弹幕口径·待官方），并保留溯源（→ 详 §N）。
    b. 禁止把章节号/章节标题碎片（如"3 BP 锚点与选人情报"、"10 预测验证回填明细"）、
       元数据碎片（如"灰信号留痕（入 gray_signals…）"）、纯时间线（10:56→11:28→11:32）、
       表格头/维度词（位置/维度/验证状态）当关键信息。
    c. 灰信号类条目必须保留"观众质疑·非结论"语义，禁止写成实锤。
    d. 实现与门禁：生成端 fast_intel_node prompt 原生产出；兜底
       tools/speedcard_consistency.py --fix（缺价值条目 LLM 改写）；发布前 --check 审计
       （empty / noisy / no_value 任一即阻止发布）；回归 tests/test_speedcard_consistency.py。
       （教训：G4 节点页速览卡曾混入章节标题+时间线且无价值句，
       详见 knowledge/INTEL_HTML_TEMPLATE.md 二.10bis。）
16. 情报来源分层原则（2026-08-26 用户定稿，最高优先级，证据层总纲）：
    a. 核心来源：每场比赛/每个节点的情报，核心必须来自**本场比赛的弹幕情报**
       （真实弹幕依据），严禁无中生有；
    b. 补充来源（必须显式标注来源）：同场前几局的弹幕与表现、队伍历史画像/
       能力（跨场积累）、盘口佐证；
    c. 合理推测：仅可基于前局/已发生事件的既定表现做适当推测，且必须标注
       「推测·待验证」，与事实分离呈现；
    d. 红线：仅有简单推断、无本场/前局/历史数据支撑的情报一律不写；
       缺源显式标注（如 SOOP 离线=缺口），禁止用历史画像冒充本场数据；
       历史画像引用须带时间；单源=待验证、多源共振=确认、方向背离=分歧。
    e. 实现：生成端 prompt 强制「每条结论带证据来源标签（本场弹幕/前局弹幕/
       历史画像/盘口）+ 推测显式标注」；溯源段逐条对应。
17. 队名歧义防误（2026-08-26 固化，最高优先级）：
    英文俚语/谐音不得当作战队信号——如 "OKBRO"、"bro tax" 是社区玩梗，不是
    BRO 战队相关弹幕；韩文/英文黑话需双语映射登记后才可入信号层
    （教训：OKBRO 曾险些被当作 BRO 战队共识）。
18. 终局判定结构源仲裁（2026-08-26 固化，最高优先级）：
    比赛终局结果发布前，必须用 Polymarket 结算价 / 官方比分源仲裁；
    弹幕"一波了/结束了/拿下/恭喜X"只能作候选信号，禁止凭推进阶段弹幕提前判终局。
    多端结果不一致时，以 Polymarket 结算价为最终仲裁，立即修正所有发布物。
    （教训：KT-BRO G5 曾凭"一波了/恭喜BRO"误判 BRO 3-2，实际 KT 3-2
    鳄鱼偷家，Polymarket lol-kt-bro2 结算 KT 99.95c。）
19. 禁跨联赛混源（2026-08-26 固化，最高优先级）：
    每个比赛的切片/情报只能使用该联赛的默认采集集（leagues.json），
    禁止把其他联赛直播间（如 LoL 房）弹幕混入 CS 板块；切片前必须过
    league_files 联赛源过滤；若发生混源，该场情报整场作废（void_match_intel.py），
    不得局部修补。缺情报的比赛直接标注"无/此节点暂未采集数据"，禁止硬编硬凑。
    （教训：Aurora vs G2（CS2）曾混入 KT vs BRO（LoL）弹幕，整场作废。）
20. 小局结束判定必须覆盖"价格即结算"（2026-08-26 固化，最高优先级）：
    Polymarket 小局市场结算后常保持 closed=false、但 outcomePrice 已定价 99.95c；
    game_status 快照（每 5 分钟）存在滞后。流水线判定小局结束时，必须同时接受：
    closed=true / winner 已置位 / 任一 outcomePrice ≥0.99 三种信号，禁止只等
    closed/winner（否则局后节点延迟 15-25 分钟）。
    （教训：NAVI-M80 G1 15:07 结束，game_status 快照 winner=null（93.5c 滞后），
    g1_end 未自动触发，由线上端本地补发。）
21. 时间轴壳与节点强一致（2026-08-27 固化，最高优先级）：
    比赛情报节点一旦生成/发布，该场时间轴壳（match_<slug>.html）必须同步包含
    新节点入口；禁止"页面已上线但入口显示暂未采集"。
    a. 发布兜底：vps_publish 每次发布前以 reports 为准重建全部比赛时间轴壳
       （rebuild_match_shells，含 matches.json + matches_today），保证壳永远最新；
    b. 节点生成侧：generate_game_node 成功后立即重建本场壳（已有）；
    c. 本地补发节点时：同步重建壳并推送，禁止只推节点页不推壳；
    d. 验证：发布后抽查线上壳入口与实际页面文件存在性一致。
    （教训：2026-08-27 NS-FOX G2 BP 页已上线但时间轴壳被旧版覆盖，
    入口显示"此节点暂未采集数据"，用户无法进入。）
22. 事实层走官方源（2026-08-27 固化，最高优先级）：
    "选手×英雄对照"、"比赛结果/比分"属于事实层，**只信官方/权威公开源**；
    弹幕是情绪与共识层，提及/讨论 ≠ 实际选人，禁止用弹幕做阵容配对。
    a. 官方 LoL 数据：esports-api.lolesports.com（赛程/详情）+ feed.lolesports.com
       window（局中阵容，开局后即有；BP 阶段返回 204 属正常待开局），
       工具 tools/fetch_official_game_data.py，leagueId 见 knowledge/OFFICIAL_DATA_SOURCES.md；
    b. 结果判定优先级：Polymarket 结算 > 官方 API > 官方直播间标题 >
       中文战报（直播吧/虎扑）> 弹幕情绪（弹幕禁止单独定胜负）；
    c. 开局后 1 分钟内用官方 window 拉阵容校准；小局结束拉 gameWins 回填；
       整场 Polymarket 结算仲裁；
    d. 所有结论保留来源标签 + 抓取时间；缺源显式标注缺口。
    （教训：2026-08-27 弹幕"狐狸"实为 BFX VicLa 的阿狸，曾被误配给 NS Scout，
    官方 window 数据与战报一致可根除此类错误。）
24. CS2 事实层走公开数据源（2026-08-27 固化，最高优先级）：
    CS2 无统一官方 API，按"分层拼装"获取事实层（开赛时间/队伍/地图/比分）：
    a. P0 Liquipedia CS（免费可脚本化）：tools/fetch_cs2_liquipedia.py，
       一行命令当天全部场次（时间/队伍/逐图比分/finished/HLTV id/VOD）；
    b. P1 HLTV（权威：阵容/地图禁选/排名；Cloudflare 反爬，走封装或人工抽查）；
    c. P2 赛事官方（blast.tv / ESL / EWC 官网 + 规则书）；
    d. P3 中文快报（直播吧 / 虎扑 / 5EPlay / 完美世界电竞）；
    e. 关键交叉纪律：**Liquipedia 比分可能滞后数分钟**（2026-08-27 IC vs VIT
       Cache 曾显示 12:12，实际 16:13 加时）——终局比分必须 HLTV/战报/官方交叉，
       时间与队伍基本准确可直接用；
    f. CS2 必备事实项：地图池与禁选（veto，弹幕"队伍×地图强图"必须交叉）、
       逐图比分含加时、赛制晋级含义、阵容完整度（stand-in）、选手角色/状态、
       HLTV 排名/H2H/地图胜率、官方规则书与赛事级别。
    （验证：2026-08-27 IC 2-0 VIT 爆冷，17:00 CST 开赛，Anubis 13:8 / Cache 16:13；
     LQ 时间/队伍/图一准，Cache 滞后印证交叉纪律。）
23. 节点切片窗口精确（2026-08-27 强化，最高优先级）：
    每个节点（赛前/BP 后/局中/局末）的情报只使用**本节点阶段时间窗**的弹幕，
    禁止混入上一局尾段、局间闲聊、更早节点数据。
    a. 切片起点 = 本节点阶段起点（如 G3 BP 节点从 G3 选人/开局开始，
       不含 G2 结束后的局间）；用弹幕密度突变/官方 schedule 校准确认阶段边界；
    b. 局间与赛前等待期弹幕不得计入下一局 BP/局中节点；
    c. 补充来源（前局表现/历史画像）可引用但必须标"前局延续/历史库"，
       不得作为本节点核心信号；
    d. 节点页数据窗口必须与切片一致并显式标注（窗口起止），供审计。
    （教训：2026-08-27 NS-FOX G3 BP 切片曾从 09:08 起（G2 结束 09:15 之前），
    混入 G2 尾段/局间弹幕；已重切为 09:22-09:44。）
16. 队名歧义防误（2026-08-26 起，最高优先级）：
    a. 英文俚语与战队名必须消歧：Twitch 聊天 "OKBRO / bro tax / BROOOO /
       BROLIEVERS / ok bro" 等多为英语俚语/聊天梗，**不得**作为 BRO 战队
       信号；"BRO 赢/拿下"必须出现显式队伍归属陈述（如 "BRION win"、
       "BRO 拿下/赢了"）才可计为战队信号。
    b. 结果声明前置条件：无结构源（比分机器/官方/积分榜/用户确认）时，
       结果一律标"待确认"，禁止凭弹幕情绪+叙事惯性声明胜负。
    c. 多信号矛盾时先列反证："我还想着 X 输" = X 未输；反向陈述必须
       纳入判定，禁止只挑支持自己假设的信号。
       （教训：KT-BRO G1 把英文俚语 bro 当 BRO 战队信号，加上
       "BRO 占优"叙事惯性，误判 BRO 1-0，实际 KT 1-0。）
17. 情报来源分层原则（2026-08-26 起，最高优先级，情报来源总纲）：
    a. **核心来源**：每场比赛 / 每个节点的情报，核心必须来自
       **本场比赛的弹幕数据**（本场弹幕 = 唯一直接证据层）；
    b. **补充来源**：① 同一系列前局的表现与弹幕（多局赛制可引用前局）；
       ② 队伍历史情报库（队伍能力 / 画像 / 联赛规律 / 灰信号实体）。
       使用补充来源前必须校验时效——阵容变化 / 版本更新 / 状态波动
       会使旧画像失效（如 KT 宫斗换 AD 后旧画像不可直接套用）；
    c. **合理推测**：仅允许基于"已发生事件"的简单外推，必须条件化表达
       （"若 X 则 Y"）并标注"推测"；禁止因果虚构（灰信号 ≠ 假赛结论）、
       禁止无依据断言；
    c2. **外推三型（2026-08-26 用户确认）**：简单外推 / 条件式外推 /
        因果外推均允许，但必须**明确外推条件**：
        · 简单外推：基于单局既定表现（"前两局放资源 → 本局倾向"），
          需注明依据局次；
        · 条件式外推："若 X 则 Y"（如"若 KT 再领先则可能再放资源"），
          条件必须可观察；
        · 因果外推：允许陈述因果链，但必须给出可溯源依据链
          （"因 A（前局证据）→ B（机制/规律）→ 预期 C"），
          禁止无条件的因果断言；灰信号 → 假赛结论永远禁止；
        所有推测标注"推测"并进入赛后验证闭环。
    d. **置信度标签**：每条情报标来源层级（本场弹幕 / 前局延续 / 历史库 /
       推测）+ 置信（多源确认 / 单源待验证 / 弹幕口径·待官方）；
    d2. **冲突优先级（2026-08-26 用户确认）**：
        ① 本场弹幕 > 历史库（时效优先）；
        ② 同场多路冲突 → 并列标注，不硬断；
        ③ 赛果判定官方源 > 弹幕（规则 14）；
        ④ 推测与既定事实冲突 → 推测自动降级并标注。
    e. **无中生有零容忍**：无证据断言、观众建议当实际选角、俚语当战队信号、
       简单推断当既定事实——一律禁止；证据引用要求见 INTEL_HTML_TEMPLATE.md
       第 12 条，审计工具与回归测试锁定。
    f. **推理闭环（2026-08-26 用户确认）**：推测类情报必须进入赛后验证
       闭环（同预测验证），统计推测命中率；未回填的推测在页面标注"待验证"。
    g. **生成端审计（2026-08-26 用户确认）**：速览卡 / 方向板的每条信号
       必须有来源标签，缺来源 = 阻止发布（生成门禁，同 match_state_guard 做法）。
18. 终局判定结构源仲裁（2026-08-26 起，最高优先级）：
    a. 比赛"终局结果"发布前，必须用 Polymarket 结算价 / 官方比分源仲裁；
       弹幕"一波了 / 结束了 / 拿下 / 恭喜X"只能作候选信号，不能作最终结果；
    b. 弹幕终局信号必须**持续确认到比赛真结束**（基地爆炸 / 官方结算 /
       Polymarket 收敛到 99+），禁止凭推进阶段弹幕提前判终局；
    c. 多对话 / 多端结果不一致时，以 Polymarket 结算价为最终仲裁，
       并立即修正所有发布物（页面 / matches.json / 复盘）。
       （教训：KT-BRO G5 20:38 弹幕"一波了/恭喜bro"提前判 BRO 3-2，
       实际 20:45 KT 鳄鱼偷家翻盘 KT 3-2——Polymarket lol-kt-bro2
       结算 KT 99.95c 为最终仲裁，线上对话判定正确、本端判反。）

## 数据同步约定（2026-08-18 起，全项目通用）

赔率/价格数据一旦更新（新快照、新复盘、新交易、其他会话产出），**派生数据必须同步更新**，
禁止"数据入库但库不更新"；跨会话接手时先 diff 最近文件，再执行本约定。

```text
1. 快照命名兼容：统计工具（pattern_audit / classify / daily_review）必须同时识别
   旧格式 game1_we_price_1m.jsonl 与新格式 slug__N__Team.jsonl；
   新增采集工具必须沿用或登记命名规范，改格式前先跑回归（漏抓 = 数据滞后告警）。
2. 数据到达即同步：
   - 新快照入库 -> 跑 classify_pattern + 更新巡检基线（pattern_audit_baseline.json）；
   - 新复盘/交易 -> 复盘闭环入库（knowledge/reviews + trades），并按 DO/DON'T 勾选；
   - 以上两类都到 -> 同步形态库频率/样本、策略库/DO-DONT 样本、HTML 频率卡、PROJECT_PROGRESS。
3. 每日例行：tools/daily_pattern_review.py（巡检 + 新内容扫描 + 连续无输入预警）。
4. 新品类/新市场类型（如 Valorant、O/U 总局数、让分、弹幕 comments）：
   必须登记到 LEAGUE_PROFILES / 形态库 / watchlist 词表，并跑一次分类验证后再使用。
5. 跨会话产出（其他窗口完成的复盘/分析/数据）：以 PROJECT_PROGRESS 为进度中枢，
   接手会话先 diff 最近文件（find -mtime），再执行第 2 条同步流程；
   弹幕/主观情报按 DANMU_README 标准工作流落盘后同样纳入同步。
6. 滞后自检：若快照/复盘日期晚于巡检基线日期 -> 视为数据滞后，告警并补跑；
   不得以"数据可能没更新"跳过（与防错规则第 1 条同一原则：先怀疑工具）。
```
